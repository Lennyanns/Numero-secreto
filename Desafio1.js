/*Crea una función que calcule el índice de masa corporal (IMC) de una persona a partir de su altura en metros y peso en kilogramos, que se recibirán como parámetros.*/
//Crea una función que calcule el valor del factorial de un número pasado como parámetro.
/*function fact(numero){
    let resultado=1;
    for(let i = 1; i<=numero; i++){
        resultado = resultado*i;
    }
    return resultado;

}
let resu=0;
let factorialnumero=fact(resu);
console.log(factorialnumero);*/

/*Crea una función que convierta un valor en dólares, pasado como parámetro,
 y devuelva el valor equivalente en reales(moneda brasileña,si deseas puedes hacerlo con el valor
 del dólar en tu país).
  Para esto, considera la cotización del dólar igual a R$4,80.*/
  /*function conver(valor){
    return valor*4.80;
  }
  let equivalente= conver(100);
  console.log(equivalente);*/

/*Crea una función que muestre en pantalla el área y el perímetro de una sala rectangular, 
utilizando la altura y la anchura que se proporcionarán como parámetros.*/
/*function mostrar(altura , ancho){
    let perimetro;
    perimetro= altura+ancho*2;
    let area;
    area=altura*ancho;
     console.log(`El perimetro de:${altura} y ${ancho} es: ${perimetro} y el area es: ${area} `);
    
}
let perimetro=mostrar(4,4);*/

/*Crea una función que muestre en pantalla el área y el perímetro de una sala circular,f 
utilizando su radio que se proporcionará como parámetro. Considera Pi = 3,14.*/
/*function sala(radio){
    const pi = 3.14159;
    const constante = 2;
    let area=pi*radio**2;
    let perimetro= constante*pi*radio;
    console.log(`El area de la sala circular es : ${area}, el perimetro de la sala circular es: ${perimetro}`);
}
let resultado= sala(5);*/




/*Crea una función que muestre en pantalla la tabla de multiplicar de un número 
dado como parámetro. */
/*function muestre(numero){
    contador=1;
     
    for(let i = 1; i<11;i++){
        contador=numero*i;
        console.log(`${i}X${numero}=${contador}`);
    }
    
}
let = resultado = muestre(5);
console.log(resultado);
*/

/*
/*Crea una lista vacía llamada "listaGenerica".*/
// let listaGenerica=[];
// console.log(listaGenerica);
/*Crea una lista de lenguajes de programación llamada "lenguagesDeProgramacion con los siguientes elementos: 'JavaScript', 'C', 'C++', 'Kotlin' y 'Python'.*/
// let lenguajesDeProgramacion = ['JavaScript', 'C', 'C++', 'Kotlin' , 'Python']
// console.log(lenguajesDeProgramacion);/*Agrega a la lista "lenguagesDeProgramacion los siguientes elementos: 'Java', 'Ruby' y 'GoLang'.*/
// lenguajesDeProgramacion.push('Java', 'Ruby','GoLang');
//  alert(console.log(lenguajesDeProgramacion.length));

/*Crea una función que muestre en la consola todos los elementos de la lista "lenguagesDeProgramacion.*/
//console.log(lenguajesDeProgramacion);
/*Crea una función que muestre en la consola todos los elementos de la lista "lenguagesDeProgramacion en orden inverso.*/
// for(let i=lenguajesDeProgramacion.length; i>0;i--){
//     console.log(lenguajesDeProgramacion[i]);
// }





/*Crea una función que calcule el promedio de los elementos en una lista de números.*/

// function promedio(){
//     let elementos=[7,9,8];
//     for(let i=0; i<elementos.length; i++){
//         let promed;
//         return promed=(elementos[0]+(elementos[1])+elementos[2])/3;
//     }

// }
// alert('='+promedio());


/*Crea una función que muestre en la consola el número más grande y el número más pequeño en una lista.*/

/*function encontrarExtremos(lista) {
    if (lista.length === 0) {
        console.log("La lista está vacía.");
        return;
    }

    var minimo = lista[0];
    var maximo = lista[0];

    for (var i = 1; i < lista.length; i++) {
        if (lista[i] < minimo) {
            minimo = lista[i];
        }
        if (lista[i] > maximo) {
            maximo = lista[i];
        }
    }

    console.log("El número más pequeño es: " + minimo);
    console.log("El número más grande es: " + maximo);
}

// Ejemplo de uso
var numeros = [5, 10, 3, 8, 1];
encontrarExtremos(numeros);*/
/*Crea una función que devuelva la suma de todos los elementos en una lista.*/
// function devolver(lista){
//     let suma =0;

//     for(let i=0;i<lista.length;i++){
//         suma+=lista[i];
//     }
//     return suma;
    
// }
// var numeros = [3,4,5,6];
// console.log(`La suma de los arreglos es: ${devolver(numeros)}`);


/*Crea una función que devuelva la posición en la lista donde se encuentra un elemento pasado como parámetro, o -1 si no existe en la lista.*/
function posicion(lista){
    

}
/*Crea una función que reciba dos listas de números del mismo tamaño y devuelva una nueva lista con la suma de los elementos uno a uno.*/
/*Crea una función que reciba una lista de números y devuelva una nueva lista con el cuadrado de cada número.
*/ 



